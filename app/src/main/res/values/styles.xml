<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item> 
        <item  name="colorPrimaryDark">@color/colorPrimaryDark</item> 
        <item name="colorAccent">@color/colorAccent</item>
        <item name="actionOverflowButtonStyle">@style/OverFlow</item>
        <item name="android:actionButtonStyle">@style/CustomActionButtonBarTheme</item>
        <item name="android:actionBarStyle">@style/CustomActionBarTheme</item>
    </style>

    <style name="CustomActionBarTheme"
        parent="@android:style/Theme.Holo.Light.DarkActionBar">
        <item name="android:gravity">left</item>
    </style>

    <style name="CustomActionButtonBarTheme"
        parent="@android:style/Theme.Holo.Light.DarkActionBar">
        <item name="android:paddingEnd">30dp</item>
        <item name="android:gravity">left</item>
    </style>

    <style name="OverFlow" parent="@style/Widget.AppCompat.Light.ActionButton.Overflow">
        <item name="android:src">@drawable/ic_overflow_icon</item>

    </style>




</resources>
